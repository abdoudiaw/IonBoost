# Mora
This code solves the Euler equations coupled with Poisson equation for electric sheath.

1 - To compile the code with Cmake, do:
    mkidr build
    cd build
    
    cmake ../src
    cmake --build .

2 - To run the script with:
        
    lmp < inputs.in
